<Window x:Class="NexMeet.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Iniciar Sesión - NexMeet" Height="400" Width="600"
        Style="{StaticResource WindowStyle}">
    <Grid>
        <!-- Título -->
        <TextBlock Text="Iniciar Sesión" FontSize="24" FontWeight="Bold"
                   HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,20,0,0"/>

        <!-- Formulario -->
        <StackPanel VerticalAlignment="Center" HorizontalAlignment="Center" Width="300">
            <TextBlock Text="Correo:" Margin="0,10,0,0"/>
            <TextBox x:Name="txtCorreo" Width="300" Height="30"/>

            <TextBlock Text="Contraseña:" Margin="0,10,0,0"/>
            <PasswordBox x:Name="txtContrasena" Width="300" Height="30"/>

            <Button Content="Ingresar" Width="100" Height="30" Margin="0,20,0,0"
                    Click="BtnIngresar_Click" HorizontalAlignment="Center"/>
        </StackPanel>
    </Grid>
</Window>

